import { EstadoCursosPipe } from './estado-cursos.pipe';

describe('EstadoCursosPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoCursosPipe();
    expect(pipe).toBeTruthy();
  });
});
